import React from 'react';
import { useTranslation } from 'react-i18next';

const Propriety: React.FC = () => {
  const { t } = useTranslation();

  return (
    <div style={{ padding: '2rem', background: 'white', minHeight: '100vh' }}>
      <h1>Test Propriety Page</h1>
      <p>Se vedi questo messaggio, il componente base funziona.</p>
      <p>Traduzione test: {t('propriety.gallery.mainTitle')}</p>
      
      <h2>Servizi Inclusi Test</h2>
      <ul>
        <li>{t('section.includedServices.comfortList1')}</li>
        <li>{t('section.includedServices.comfortList2')}</li>
      </ul>
      
      <h2>Costi Extra Test</h2>
      <ul>
        <li>{t('section.extraCosts.mandatoryList1')}</li>
        <li>{t('section.extraCosts.mandatoryList2')}</li>
      </ul>
    </div>
  );
};

export default Propriety;